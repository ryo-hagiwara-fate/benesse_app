import 'package:flutter/material.dart';

class QuestionPostScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      body: Center(child: Text("てすと！"),),
    );
  }
}
