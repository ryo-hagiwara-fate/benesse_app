import 'package:flutter/material.dart';

class StudyPostScreen extends StatelessWidget {
  const StudyPostScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      body: Center(child: Text("てすと！"),),
    );
  }
}